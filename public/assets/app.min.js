import * as E from "three";
function ke(t, e) {
  if (!t || !e) return () => {};
  let n = window.matchMedia("(prefers-reduced-motion: reduce)").matches,
    o = new E.WebGLRenderer({
      canvas: t,
      antialias: !1,
      alpha: !0,
      powerPreference: "high-performance",
    });
  (o.setPixelRatio(Math.min(window.devicePixelRatio, 1.5)),
    (o.outputColorSpace = E.SRGBColorSpace));
  let s = new E.Scene(),
    a = new E.PerspectiveCamera(45, 1, 0.1, 100);
  (a.position.set(3.8, 2.5, 5.2), a.lookAt(0, 0, 0));
  let i = () => {
    let b = Math.max(1, e.clientWidth),
      k = Math.max(1, e.clientHeight);
    (o.setSize(b, k, !1), (a.aspect = b / k), a.updateProjectionMatrix());
  };
  i();
  let r = new E.Group();
  s.add(r);
  let d = new E.IcosahedronGeometry(1.2, 0),
    h = new E.Mesh(
      d,
      new E.MeshStandardMaterial({ color: 1052693, metalness: 0.75, roughness: 0.42 }),
    );
  (h.scale.setScalar(0.935), r.add(h));
  let c = new E.Mesh(
    d,
    new E.MeshStandardMaterial({
      color: 8018175,
      transparent: !0,
      opacity: 0.42,
      metalness: 0.18,
      roughness: 0.16,
    }),
  );
  r.add(c);
  let f = new E.LineSegments(
    new E.EdgesGeometry(d),
    new E.LineBasicMaterial({ color: 12429311, transparent: !0, opacity: 0.62 }),
  );
  (r.add(f), s.add(new E.AmbientLight(16777215, 0.78)));
  let u = new E.DirectionalLight(16777215, 1.25);
  (u.position.set(3, 4, 5), s.add(u));
  let g = new E.DirectionalLight(9133302, 0.45);
  (g.position.set(-2, 1, -3), s.add(g));
  let x = 0,
    y = !0,
    S = () => o.render(s, a);
  function M() {
    if (!y) return;
    x = requestAnimationFrame(M);
    let b = performance.now() * 0.001;
    ((r.rotation.y += 0.0055),
      (r.rotation.x = Math.sin(b * 0.5) * 0.09),
      (r.position.y = Math.sin(b * 0.9) * 0.06),
      S());
  }
  n ? S() : M();
  let L = new ResizeObserver(() => {
    (i(), S());
  });
  L.observe(e);
  let C = () => {
    document.hidden ? ((y = !1), cancelAnimationFrame(x)) : !n && !y && ((y = !0), M());
  };
  return (
    document.addEventListener("visibilitychange", C),
    () => {
      ((y = !1),
        cancelAnimationFrame(x),
        L.disconnect(),
        document.removeEventListener("visibilitychange", C),
        d.dispose(),
        h.material.dispose(),
        c.material.dispose(),
        f.geometry.dispose(),
        f.material.dispose(),
        o.dispose());
    }
  );
}
var P = [
    {
      id: "bd-passport",
      label: "Bangladesh Passport 45\xD755mm",
      country: "Bangladesh",
      widthMM: 45,
      heightMM: 55,
      dpi: 300,
      subjectFill: 0.649090909090909,
      headHeightMM: 34,
      bottomFlush: !1,
    },
    {
      id: "us-passport",
      label: 'US Passport 2\xD72"',
      country: "United States",
      widthMM: 51,
      heightMM: 51,
      dpi: 300,
      subjectFill: 0.66,
    },
    {
      id: "schengen",
      label: "Schengen / EU Visa 35\xD745mm",
      country: "Europe",
      widthMM: 35,
      heightMM: 45,
      dpi: 300,
      subjectFill: 0.72,
    },
    {
      id: "uk-passport",
      label: "UK Passport 35\xD745mm",
      country: "United Kingdom",
      widthMM: 35,
      heightMM: 45,
      dpi: 300,
      subjectFill: 0.72,
    },
    {
      id: "india-passport",
      label: "India Passport 35\xD745mm",
      country: "India",
      widthMM: 35,
      heightMM: 45,
      dpi: 300,
      subjectFill: 0.72,
    },
    {
      id: "india-2x2",
      label: 'India / OCI 2\xD72"',
      country: "India",
      widthMM: 51,
      heightMM: 51,
      dpi: 300,
      subjectFill: 0.66,
    },
    {
      id: "canada-passport",
      label: "Canada Passport 50\xD770mm",
      country: "Canada",
      widthMM: 50,
      heightMM: 70,
      dpi: 300,
      subjectFill: 0.7,
    },
    {
      id: "china-visa",
      label: "China Visa 33\xD748mm",
      country: "China",
      widthMM: 33,
      heightMM: 48,
      dpi: 300,
      subjectFill: 0.72,
    },
    {
      id: "australia-passport",
      label: "Australia Passport 35\xD745mm",
      country: "Australia",
      widthMM: 35,
      heightMM: 45,
      dpi: 300,
      subjectFill: 0.72,
    },
  ],
  Ft = [
    { id: "blue", label: "Passport Blue", color: "#2563eb" },
    { id: "white", label: "White", color: "#ffffff" },
    { id: "off-white", label: "Off White", color: "#f3f4f6" },
    { id: "light-blue", label: "Light Blue", color: "#cfe0f2" },
    { id: "sky", label: "Sky Blue", color: "#a9c7e8" },
    { id: "grey", label: "Grey", color: "#d7dbe0" },
    { id: "red", label: "Red", color: "#e23b3b" },
  ];
function Mt(t) {
  return {
    width: Math.round((t.widthMM / 25.4) * t.dpi),
    height: Math.round((t.heightMM / 25.4) * t.dpi),
  };
}
var vn = "https://huggingface.co/Xenova/modnet/resolve/main/onnx/model_fp16.onnx",
  mo = 512,
  go = 32,
  Ut = null,
  et = null,
  bn = "makepics-models",
  Lt = "onnx",
  Pe = "modnet-fp16-v1";
function Te() {
  return new Promise((t, e) => {
    if (typeof indexedDB > "u") return e(new Error("no idb"));
    let n = indexedDB.open(bn, 1);
    ((n.onupgradeneeded = () => n.result.createObjectStore(Lt)),
      (n.onsuccess = () => t(n.result)),
      (n.onerror = () => e(n.error)));
  });
}
async function En() {
  try {
    let t = await Te();
    return await new Promise((e, n) => {
      let s = t.transaction(Lt, "readonly").objectStore(Lt).get(Pe);
      ((s.onsuccess = () => e(s.result || null)), (s.onerror = () => n(s.error)));
    });
  } catch {
    return null;
  }
}
async function xn(t) {
  try {
    let e = await Te();
    await new Promise((n, o) => {
      let s = e.transaction(Lt, "readwrite");
      (s.objectStore(Lt).put(t, Pe), (s.oncomplete = () => n()), (s.onerror = () => o(s.error)));
    });
  } catch {}
}
async function Sn(t) {
  if (et) return et;
  let e = await En();
  if (e && e.byteLength)
    return (
      (et = e instanceof Uint8Array ? e : new Uint8Array(e)),
      t && t({ stage: "download", loaded: et.length, total: et.length }),
      et
    );
  let n = await fetch(vn);
  if (!n.ok || !n.body) throw new Error(`Model download failed (${n.status})`);
  let o = Number(n.headers.get("content-length")) || 0,
    s = n.body.getReader(),
    a = [],
    i = 0;
  for (;;) {
    let { done: h, value: c } = await s.read();
    if (h) break;
    (a.push(c), (i += c.length), t && t({ stage: "download", loaded: i, total: o }));
  }
  let r = new Uint8Array(i),
    d = 0;
  for (let h of a) (r.set(h, d), (d += h.length));
  return ((et = r), xn(r), r);
}
function Mn(t) {
  return (
    Ut ||
    ((Ut = (async () => {
      let e = await import("onnxruntime-web/webgpu");
      e.env.wasm.wasmPaths = "https://cdn.jsdelivr.net/npm/onnxruntime-web@1.27.0/dist/";
      let n = (typeof navigator < "u" && navigator.hardwareConcurrency) || 1;
      e.env.wasm.numThreads = globalThis.crossOriginIsolated ? Math.min(4, n) : 1;
      let o = await Sn(t);
      t && t({ stage: "compile" });
      let s,
        a = "wasm";
      if (typeof navigator < "u" && "gpu" in navigator)
        try {
          ((s = await e.InferenceSession.create(o, {
            executionProviders: ["webgpu"],
            graphOptimizationLevel: "all",
          })),
            (a = "webgpu"));
        } catch {
          s = void 0;
        }
      return (
        s ||
          ((s = await e.InferenceSession.create(o, {
            executionProviders: ["wasm"],
            graphOptimizationLevel: "all",
          })),
          (a = "wasm")),
        t && t({ stage: "ready" }),
        { ort: e, session: s, backend: a }
      );
    })()),
    Ut)
  );
}
function Ln(t) {
  let e = t.naturalWidth || t.width || 0,
    n = t.naturalHeight || t.height || 0;
  return { w: e, h: n };
}
function Cn(t, e) {
  let n = t,
    o = e;
  return (
    (Math.max(t, e) > 512 || Math.min(t, e) < 512) &&
      (t >= e
        ? ((n = 512), (o = Math.round((e / t) * 512)))
        : ((o = 512), (n = Math.round((t / e) * 512)))),
    (n = Math.max(32, n - (n % 32))),
    (o = Math.max(32, o - (o % 32))),
    { rw: n, rh: o }
  );
}
async function ae(t, e) {
  let { ort: n, session: o, backend: s } = await Mn(e),
    { w: a, h: i } = Ln(t),
    { rw: r, rh: d } = Cn(a, i),
    h = document.createElement("canvas");
  ((h.width = r), (h.height = d));
  let c = h.getContext("2d", { willReadFrequently: !0 });
  ((c.imageSmoothingEnabled = !0), (c.imageSmoothingQuality = "high"), c.drawImage(t, 0, 0, r, d));
  let { data: f } = c.getImageData(0, 0, r, d),
    u = r * d,
    g = new Float32Array(3 * u);
  for (let I = 0; I < u; I++)
    ((g[I] = f[I * 4] / 127.5 - 1),
      (g[u + I] = f[I * 4 + 1] / 127.5 - 1),
      (g[2 * u + I] = f[I * 4 + 2] / 127.5 - 1));
  let x = o.inputNames[0],
    y = o.outputNames[0],
    S = new n.Tensor("float32", g, [1, 3, d, r]),
    M = performance.now(),
    L = await o.run({ [x]: S }),
    C = performance.now() - M,
    b = L[y].data,
    k = document.createElement("canvas");
  ((k.width = r), (k.height = d));
  let R = k.getContext("2d"),
    $ = R.createImageData(r, d);
  for (let I = 0; I < u; I++) {
    let W = b[I];
    ((W = W < 0 ? 0 : W > 1 ? 1 : W),
      ($.data[I * 4] = 255),
      ($.data[I * 4 + 1] = 255),
      ($.data[I * 4 + 2] = 255),
      ($.data[I * 4 + 3] = W * 255));
  }
  return (R.putImageData($, 0, 0), { maskCanvas: k, backend: s, inferenceMs: C });
}
function Be(t, e) {
  let n = t.width,
    o = t.height;
  if (!n || !o) return null;
  let s = 200,
    a = Math.max(1, Math.round((o / n) * s)),
    i = document.createElement("canvas");
  ((i.width = s), (i.height = a));
  let r = i.getContext("2d", { willReadFrequently: !0 });
  r.drawImage(t, 0, 0, s, a);
  let h = r.getImageData(0, 0, s, a).data,
    c = null;
  if (e) {
    let D = document.createElement("canvas");
    ((D.width = s), (D.height = a));
    let v = D.getContext("2d", { willReadFrequently: !0 });
    (v.drawImage(e, 0, 0, s, a), (c = v.getImageData(0, 0, s, a).data));
  }
  let f = -1,
    u = -1,
    g = s,
    x = 0;
  for (let D = 0; D < a; D++)
    for (let v = 0; v < s; v++) {
      let B = (D * s + v) * 4;
      (c ? c[B + 3] : h[B + 3]) > 80 &&
        (f === -1 && (f = D), (u = D), v < g && (g = v), v > x && (x = v));
    }
  if (f === -1) return null;
  let y = u - f + 1,
    S = Math.min(a - 1, Math.round(f + y * 0.35)),
    M = s,
    L = 0,
    C = 0,
    b = 0;
  for (let D = f; D <= S; D++)
    for (let v = 0; v < s; v++) {
      let B = (D * s + v) * 4;
      (c ? c[B + 3] : h[B + 3]) > 80 && (v < M && (M = v), v > L && (L = v), (C += v), b++);
    }
  let k = Math.max(10, L - M),
    R = b > 0 ? C / b : (g + x) / 2,
    $ = new Uint8Array(s * a),
    I = 0,
    W = 0;
  for (let D = f; D <= Math.min(a - 1, u); D++)
    for (let v = M; v <= L; v++) {
      let B = (D * s + v) * 4;
      if (c && c[B + 3] < 80) continue;
      let O = h[B],
        St = h[B + 1],
        Ot = h[B + 2];
      O > 60 &&
        St > 40 &&
        Ot > 20 &&
        Math.max(O, St, Ot) - Math.min(O, St, Ot) > 12 &&
        Math.abs(O - St) > 10 &&
        O > St &&
        O > Ot &&
        (($[D * s + v] = 1), I++, (W += v));
    }
  let ne = -1,
    oe = I > 20 ? W / I : R;
  if (I > 20) {
    let D = f;
    for (let v = f; v <= u; v++) {
      let B = 0;
      for (let O = Math.max(0, Math.floor(oe - 25)); O <= Math.min(s - 1, Math.ceil(oe + 25)); O++)
        $[v * s + O] && B++;
      B >= 3 && (D = v);
    }
    ne = Math.min(u, D);
  }
  let fn = ne > f + 10 ? ne - f : k * 1.28,
    hn = Math.min(y * 0.52, k * 1.35),
    pn = Math.max(15, k * 1.1),
    mn = Math.min(hn, Math.max(pn, fn)),
    gn = n / s,
    Ce = o / a,
    Ie = f * Ce,
    De = mn * Ce,
    wn = Ie + De,
    yn = oe * gn;
  return { headTopY: Ie, chinY: wn, faceHeight: De, faceCenterX: yn, fullW: n, fullH: o };
}
var Ct = { zoom: 1, offsetX: 0, offsetY: 0 };
function se(t, e, n, o) {
  let s = document.createElement("canvas");
  ((s.width = n), (s.height = o));
  let a = s.getContext("2d");
  ((a.imageSmoothingEnabled = !0),
    (a.imageSmoothingQuality = "high"),
    a.drawImage(t, 0, 0, n, o),
    (a.globalCompositeOperation = "destination-in"),
    a.drawImage(e, 0, 0, n, o),
    (a.globalCompositeOperation = "source-over"));
  let i = 160,
    r = Math.max(1, Math.round((o / n) * i)),
    d = document.createElement("canvas");
  ((d.width = i), (d.height = r));
  let h = d.getContext("2d", { willReadFrequently: !0 });
  h.drawImage(e, 0, 0, i, r);
  let c = h.getImageData(0, 0, i, r).data,
    f = i,
    u = r,
    g = 0,
    x = 0,
    y = !1,
    S = 96;
  for (let C = 0; C < r; C++)
    for (let b = 0; b < i; b++)
      c[(C * i + b) * 4 + 3] > S &&
        ((y = !0), b < f && (f = b), b > g && (g = b), C < u && (u = C), C > x && (x = C));
  let M;
  if (y) {
    let C = n / i,
      b = o / r,
      k = f * C,
      R = u * b,
      $ = (g - f + 1) * C,
      I = (x - u + 1) * b;
    M = { x: k, y: R, w: $, h: I, cx: k + $ / 2 };
  } else M = { x: 0, y: 0, w: n, h: o, cx: n / 2 };
  let L = Be(t, e);
  return { canvas: s, width: n, height: o, bbox: M, face: L };
}
function dt(t, e, n, o) {
  let { width: s, height: a } = Mt(e),
    i = s / a,
    r,
    d,
    h;
  t.face && t.face.faceHeight > 15
    ? ((r = t.face.headTopY), (d = t.face.faceCenterX), (h = t.face.faceHeight))
    : t.bbox
      ? ((r = t.bbox.y), (d = t.bbox.cx), (h = Math.min(t.bbox.h * 0.45, t.bbox.w * 0.85)))
      : ((r = 0), (d = t.width / 2), (h = t.height * 0.4));
  let c = e.subjectFill || (34 / 55) * 1.05,
    u = h / c / (o.zoom || 1),
    g = u * i,
    x = (e.id === "bd-passport", 0.08),
    y = d - g / 2 + (o.offsetX || 0) * g,
    S = r - x * u + (o.offsetY || 0) * u,
    M = document.createElement("canvas");
  ((M.width = s), (M.height = a));
  let L = M.getContext("2d");
  return (
    (L.fillStyle = n),
    L.fillRect(0, 0, s, a),
    (L.imageSmoothingEnabled = !0),
    (L.imageSmoothingQuality = "high"),
    o.rotate
      ? (L.save(),
        L.translate(s / 2, a / 2),
        L.rotate((o.rotate * Math.PI) / 180),
        L.drawImage(t.canvas, y, S, g, u, -s / 2, -a / 2, s, a),
        L.restore())
      : L.drawImage(t.canvas, y, S, g, u, 0, 0, s, a),
    M
  );
}
function Eo(t, e) {
  let n = e.dpi,
    { width: o, height: s } = Mt(e),
    a = Math.round(0.04 * n),
    i = Math.round(0.15 * n),
    r = 6 * n,
    d = 4 * n,
    h = (C, b) => {
      let k = Math.max(1, Math.floor((C - 2 * i + a) / (o + a))),
        R = Math.max(1, Math.floor((b - 2 * i + a) / (s + a)));
      return { sheetW: C, sheetH: b, cols: k, rows: R, count: k * R };
    },
    c = h(d, r),
    f = h(r, d),
    u = f.count > c.count ? f : c,
    g = document.createElement("canvas");
  ((g.width = u.sheetW), (g.height = u.sheetH));
  let x = g.getContext("2d");
  ((x.fillStyle = "#ffffff"), x.fillRect(0, 0, u.sheetW, u.sheetH));
  let y = u.cols * o + (u.cols - 1) * a,
    S = u.rows * s + (u.rows - 1) * a,
    M = Math.round((u.sheetW - y) / 2),
    L = Math.round((u.sheetH - S) / 2);
  ((x.strokeStyle = "#c8c8c8"), (x.lineWidth = 1));
  for (let C = 0; C < u.rows; C++)
    for (let b = 0; b < u.cols; b++) {
      let k = M + b * (o + a),
        R = L + C * (s + a);
      (x.drawImage(t, k, R, o, s), x.strokeRect(k + 0.5, R + 0.5, o, s));
    }
  return g;
}
function Ht(t, e, n) {
  return new Promise((o, s) => {
    t.toBlob((a) => (a ? o(a) : s(new Error("Export failed"))), e, n);
  });
}
function $t(t, e) {
  let n = URL.createObjectURL(t),
    o = document.createElement("a");
  ((o.href = n),
    (o.download = e),
    document.body.appendChild(o),
    o.click(),
    o.remove(),
    setTimeout(() => URL.revokeObjectURL(n), 1e3));
}
var ut = [
    "brightness",
    "contrast",
    "exposure",
    "highlights",
    "shadows",
    "clarity",
    "sharpness",
    "noise",
  ],
  It = Object.fromEntries(ut.map((t) => [t, 0])),
  Ae = "makepics.edits.v1";
function Fe() {
  try {
    let t = localStorage.getItem(Ae);
    if (!t) return { ...It };
    let e = JSON.parse(t),
      n = { ...It };
    for (let o of ut) typeof e[o] == "number" && (n[o] = F(e[o], -100, 100));
    return n;
  } catch {
    return { ...It };
  }
}
function ie(t) {
  try {
    localStorage.setItem(Ae, JSON.stringify(t));
  } catch {}
}
function In(t) {
  return ut.every((e) => (t[e] || 0) === 0);
}
function F(t, e, n) {
  return t < e ? e : t > n ? n : t;
}
function Ue(t, e) {
  let n = t.width,
    o = t.height,
    s = document.createElement("canvas");
  ((s.width = n), (s.height = o));
  let a = s.getContext("2d");
  if ((a.drawImage(t, 0, 0), In(e))) return s;
  let i = a.getImageData(0, 0, n, o),
    r = i.data,
    d = Dn(e);
  for (let u = 0; u < r.length; u += 4)
    r[u + 3] !== 0 && ((r[u] = d[r[u]]), (r[u + 1] = d[r[u + 1]]), (r[u + 2] = d[r[u + 2]]));
  let h = Number(e.noise) || 0;
  if (h > 0) {
    let u = Math.max(1, Math.round((h / 100) * 3));
    Re(r, n, o, u);
  }
  let c = Number(e.sharpness) || 0;
  if (c !== 0) {
    let u = c / 100;
    Oe(r, n, o, u, 1);
  }
  let f = Number(e.clarity) || 0;
  if (f !== 0) {
    let u = f / 100;
    Oe(r, n, o, u * 0.6, 4, !0);
  }
  return (a.putImageData(i, 0, 0), s);
}
function Dn(t) {
  let e = (t.brightness || 0) * 1.27,
    n = Math.pow(2, (t.exposure || 0) / 100),
    o = 1 + (t.contrast || 0) / 100,
    s = (t.highlights || 0) / 100,
    a = (t.shadows || 0) / 100,
    i = (t.whites || 0) / 100,
    r = (t.blacks || 0) / 100,
    d = new Uint8ClampedArray(256);
  for (let h = 0; h < 256; h++) {
    let c = h / 255;
    if (((c = c * n), (c = c + e / 255), (c = (c - 0.5) * o + 0.5), s !== 0)) {
      let f = _t(0.5, 1, c);
      c += s * 0.35 * f;
    }
    if (a !== 0) {
      let f = 1 - _t(0, 0.5, c);
      c += a * 0.35 * f;
    }
    if (i !== 0) {
      let f = _t(0.7, 1.05, c);
      c += i * 0.4 * f;
    }
    if (r !== 0) {
      let f = 1 - _t(-0.05, 0.3, c);
      c += r * 0.4 * f;
    }
    d[h] = Math.round(F(c, 0, 1) * 255);
  }
  return d;
}
function _t(t, e, n) {
  let o = F((n - t) / (e - t), 0, 1);
  return o * o * (3 - 2 * o);
}
function Re(t, e, n, o) {
  let s = new Uint8ClampedArray(t.length),
    a = o * 2 + 1;
  for (let i = 0; i < n; i++) {
    let r = 0,
      d = 0,
      h = 0,
      c = 0;
    for (let f = -o; f <= o; f++) {
      let u = F(f, 0, e - 1),
        g = (i * e + u) * 4;
      ((r += t[g]), (d += t[g + 1]), (h += t[g + 2]), c++);
    }
    for (let f = 0; f < e; f++) {
      let u = (i * e + f) * 4;
      ((s[u] = r / c), (s[u + 1] = d / c), (s[u + 2] = h / c), (s[u + 3] = t[u + 3]));
      let g = F(f + o + 1, 0, e - 1),
        x = F(f - o, 0, e - 1),
        y = (i * e + g) * 4,
        S = (i * e + x) * 4;
      ((r += t[y] - t[S]), (d += t[y + 1] - t[S + 1]), (h += t[y + 2] - t[S + 2]));
    }
  }
  for (let i = 0; i < e; i++) {
    let r = 0,
      d = 0,
      h = 0,
      c = 0;
    for (let f = -o; f <= o; f++) {
      let g = (F(f, 0, n - 1) * e + i) * 4;
      ((r += s[g]), (d += s[g + 1]), (h += s[g + 2]), c++);
    }
    for (let f = 0; f < n; f++) {
      let u = (f * e + i) * 4;
      ((t[u] = r / c), (t[u + 1] = d / c), (t[u + 2] = h / c));
      let g = F(f + o + 1, 0, n - 1),
        x = F(f - o, 0, n - 1),
        y = (g * e + i) * 4,
        S = (x * e + i) * 4;
      ((r += s[y] - s[S]), (d += s[y + 1] - s[S + 1]), (h += s[y + 2] - s[S + 2]));
    }
  }
}
function Oe(t, e, n, o, s, a = !1) {
  let i = new Uint8ClampedArray(t.length);
  (i.set(t), Re(i, e, n, s));
  for (let r = 0; r < t.length; r += 4) {
    if (t[r + 3] === 0) continue;
    let d = 1;
    if (a) {
      let c = (0.299 * t[r] + 0.587 * t[r + 1] + 0.114 * t[r + 2]) / 255;
      ((d = 1 - Math.abs(c - 0.5) * 2), d < 0 && (d = 0));
    }
    let h = o * d;
    ((t[r] = F(t[r] + (t[r] - i[r]) * h, 0, 255)),
      (t[r + 1] = F(t[r + 1] + (t[r + 1] - i[r + 1]) * h, 0, 255)),
      (t[r + 2] = F(t[r + 2] + (t[r + 2] - i[r + 2]) * h, 0, 255)));
  }
}
var m = (t) => document.getElementById(t),
  l = {
    isOpen: !1,
    cutout: null,
    spec: null,
    bgColor: "#2563eb",
    baseAdjust: { zoom: 1, offsetX: 0, offsetY: 0 },
    count: 4,
    selectedSlotIdx: null,
    grid: {
      sheetMM: { width: 210, height: 297 },
      marginMM: 2,
      gapMM: 2,
      cols: 5,
      rows: 6,
      totalSlots: 30,
      photoW: 35,
      photoH: 45,
    },
    slots: [],
  };
function kn(t) {
  let e = { width: 210, height: 297 },
    n = t.widthMM || 35,
    o = t.heightMM || 45,
    s = 2,
    a = 2,
    i = e.width - 2 * s,
    r = e.height - 2 * s,
    d = Math.max(1, Math.floor((i + a) / (n + a))),
    h = Math.max(1, Math.floor((r + a) / (o + a))),
    c = d * h;
  return {
    sheetMM: e,
    marginMM: s,
    gapMM: a,
    photoW: n,
    photoH: o,
    cols: d,
    rows: h,
    totalSlots: c,
  };
}
function Xe(t) {
  ((l.cutout = t.cutout),
    (l.spec = t.spec || P[0]),
    (l.bgColor = t.bgColor || "#2563eb"),
    (l.baseAdjust = t.adjust || { zoom: 1, offsetX: 0, offsetY: 0 }),
    (l.count = Math.max(1, Math.min(60, t.count || 4))),
    (l.grid = kn(l.spec)),
    $e(l.count));
  let e = m("printEditorModal");
  (e && e.classList.remove("hidden"), (l.isOpen = !0), An(), re(), U());
}
function re() {
  let t = m("printBgSwatches");
  t &&
    t.querySelectorAll(".swMini").forEach((n) => {
      n.classList.toggle("active", n.dataset.color === l.bgColor);
    });
  let e = m("printBgPicker");
  e && (e.value = l.bgColor);
}
function le() {
  let t = m("printEditorModal");
  (t && t.classList.add("hidden"), (l.isOpen = !1));
}
function He() {
  return {
    id: "p_" + Date.now() + "_" + Math.random().toString(36).substr(2, 4),
    zoom: l.baseAdjust.zoom || 1,
    offsetX: l.baseAdjust.offsetX || 0,
    offsetY: l.baseAdjust.offsetY || 0,
  };
}
function $e(t) {
  let e = l.grid.totalSlots;
  l.slots = new Array(e).fill(null);
  let n = Math.min(t, e);
  for (let o = 0; o < n; o++) l.slots[o] = He();
  l.selectedSlotIdx = n > 0 ? 0 : null;
}
function Pn() {
  let t = l.slots.filter((n) => n !== null),
    e = l.grid.totalSlots;
  l.slots = new Array(e).fill(null);
  for (let n = 0; n < Math.min(t.length, e); n++) l.slots[n] = t[n];
  l.selectedSlotIdx !== null && l.selectedSlotIdx >= e && (l.selectedSlotIdx = null);
}
function ce(t) {
  let e = l.grid.totalSlots;
  ((t = Math.max(1, Math.min(e, t))), (l.count = t));
  let n = l.slots.filter((o) => o !== null).length;
  if (t > n) {
    let o = t - n;
    for (let s = 0; s < e && o > 0; s++) l.slots[s] === null && ((l.slots[s] = He()), o--);
  } else if (t < n) {
    let o = n - t;
    for (let s = e - 1; s >= 0 && o > 0; s--)
      l.slots[s] !== null &&
        ((l.slots[s] = null), l.selectedSlotIdx === s && (l.selectedSlotIdx = null), o--);
  }
}
function Tn(t, e) {
  if (t < 0 || t >= l.slots.length || e < 0 || e >= l.slots.length) return;
  let n = l.slots[t];
  ((l.slots[t] = l.slots[e]), (l.slots[e] = n), (l.selectedSlotIdx = e));
}
function U() {
  if (!l.isOpen) return;
  let t = l.slots.filter((o) => o !== null).length;
  (m("printQtyInput") && (m("printQtyInput").value = t),
    m("statCopies") && (m("statCopies").textContent = `${t} / ${l.grid.totalSlots}`),
    m("statSpec") && (m("statSpec").textContent = `${l.spec.widthMM}\xD7${l.spec.heightMM} mm`),
    m("statGrid") &&
      (m("statGrid").textContent =
        `${l.grid.totalSlots} slots (${l.grid.cols}\xD7${l.grid.rows})`));
  let e = m("a4SlotsGrid");
  if (!e) return;
  let n = m("printSheetPage");
  (n && (n.style.aspectRatio = "210 / 297"),
    (e.style.gridTemplateColumns = `repeat(${l.grid.cols}, 1fr)`),
    (e.style.gridTemplateRows = `repeat(${l.grid.rows}, 1fr)`),
    (e.innerHTML = ""));
  for (let o = 0; o < l.grid.totalSlots; o++) {
    let s = l.slots[o],
      a = document.createElement("div");
    if (
      ((a.className = "gridSlot " + (s ? "occupied" : "empty")),
      o === l.selectedSlotIdx && a.classList.add("selected"),
      (a.dataset.slotIdx = o),
      (a.style.aspectRatio = `${l.grid.photoW} / ${l.grid.photoH}`),
      s)
    ) {
      a.draggable = !0;
      let i = dt(l.cutout, l.spec, l.bgColor, {
        zoom: s.zoom,
        offsetX: s.offsetX,
        offsetY: s.offsetY,
      });
      i.className = "printPhotoCanvas";
      let r = document.createElement("div");
      ((r.className = "photoBadge"), (r.textContent = "#" + (o + 1)));
      let d = document.createElement("div");
      ((d.className = "photoHoverOverlay"),
        (d.innerHTML = `
        <button class="photoActionBtn dup" title="Duplicate copy" data-action="dup" data-idx="${o}">
          <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><line x1="12" y1="5" x2="12" y2="19"/><line x1="5" y1="12" x2="19" y2="12"/></svg>
        </button>
        <button class="photoActionBtn del" title="Delete copy" data-action="del" data-idx="${o}">
          <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><polyline points="3 6 5 6 21 6"/><path d="M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6m3 0V4a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2"/></svg>
        </button>
      `),
        a.appendChild(i),
        a.appendChild(r),
        a.appendChild(d),
        a.addEventListener("dragstart", (h) => {
          (h.dataTransfer.setData("text/plain", o.toString()), a.classList.add("dragging"));
        }),
        a.addEventListener("dragend", () => a.classList.remove("dragging")));
    } else {
      let i = document.createElement("span");
      ((i.className = "slotEmptyLabel"), (i.textContent = `${o + 1}`), a.appendChild(i));
    }
    (a.addEventListener("dragover", (i) => {
      (i.preventDefault(), (i.dataTransfer.dropEffect = "move"));
    }),
      a.addEventListener("dragenter", (i) => {
        (i.preventDefault(), a.classList.add("drop-target"));
      }),
      a.addEventListener("dragleave", () => {
        a.classList.remove("drop-target");
      }),
      a.addEventListener("drop", (i) => {
        (i.preventDefault(), a.classList.remove("drop-target"));
        let r = i.dataTransfer.getData("text/plain");
        if (r !== "") {
          let d = parseInt(r, 10);
          !isNaN(d) && d !== o && (Tn(d, o), U());
        }
      }),
      a.addEventListener("click", (i) => {
        let r = i.target.closest(".photoActionBtn");
        if (r) {
          i.stopPropagation();
          let d = r.dataset.action,
            h = parseInt(r.dataset.idx, 10);
          d === "dup" ? de(h) : d === "del" && ue(h);
        } else ((l.selectedSlotIdx = o), U());
      }),
      e.appendChild(a));
  }
  Bn();
}
function Bn() {
  let t = m("itemInspectorBox");
  if (!t) return;
  (l.selectedSlotIdx === null || l.selectedSlotIdx < 0 || l.selectedSlotIdx >= l.slots.length) &&
    (l.selectedSlotIdx = 0);
  let e = l.slots[l.selectedSlotIdx];
  (t.classList.remove("hidden"),
    m("inspPhotoNum") && (m("inspPhotoNum").textContent = l.selectedSlotIdx + 1));
  let n = !!e;
  (m("inspDupBtn") && (m("inspDupBtn").disabled = !n),
    m("inspDelBtn") && (m("inspDelBtn").disabled = !n),
    m("topDupBtn") && (m("topDupBtn").disabled = !n),
    m("topDelBtn") && (m("topDelBtn").disabled = !n));
}
function de(t) {
  if (((t === null || t < 0 || t >= l.slots.length) && (t = 0), l.slots[t] === null)) return;
  let e = l.slots[t],
    n = l.slots.findIndex((o) => o === null);
  (n !== -1 &&
    ((l.slots[n] = {
      id: "p_" + Date.now() + "_" + Math.random().toString(36).substr(2, 4),
      zoom: e.zoom,
      offsetX: e.offsetX,
      offsetY: e.offsetY,
    }),
    (l.selectedSlotIdx = n)),
    U());
}
function ue(t) {
  ((t === null || t < 0 || t >= l.slots.length) && (t = 0), (l.slots[t] = null), U());
}
var _e = !1;
function An() {
  _e ||
    ((_e = !0),
    m("closePrintEditor") && m("closePrintEditor").addEventListener("click", le),
    m("cancelPrintEditor") && m("cancelPrintEditor").addEventListener("click", le),
    m("printQtyDec") &&
      m("printQtyDec").addEventListener("click", () => {
        let t = l.slots.filter((e) => e !== null).length;
        t > 1 && (ce(t - 1), U());
      }),
    m("printQtyInc") &&
      m("printQtyInc").addEventListener("click", () => {
        let t = l.slots.filter((e) => e !== null).length;
        t < l.grid.totalSlots && (ce(t + 1), U());
      }),
    m("printQtyInput") &&
      m("printQtyInput").addEventListener("change", (t) => {
        let e = parseInt(t.target.value, 10) || 1;
        (ce(e), U());
      }),
    m("printAutoArrange") &&
      m("printAutoArrange").addEventListener("click", () => {
        (Pn(), U());
      }),
    m("printResetLayout") &&
      m("printResetLayout").addEventListener("click", () => {
        ($e(4), U());
      }),
    m("inspDupBtn") &&
      m("inspDupBtn").addEventListener("click", () => {
        de(l.selectedSlotIdx);
      }),
    m("inspDelBtn") &&
      m("inspDelBtn").addEventListener("click", () => {
        ue(l.selectedSlotIdx);
      }),
    m("topDupBtn") &&
      m("topDupBtn").addEventListener("click", () => {
        de(l.selectedSlotIdx);
      }),
    m("topDelBtn") &&
      m("topDelBtn").addEventListener("click", () => {
        ue(l.selectedSlotIdx);
      }),
    m("printBgSwatches") &&
      m("printBgSwatches").addEventListener("click", (t) => {
        let e = t.target.closest(".swMini");
        e && e.dataset.color && ((l.bgColor = e.dataset.color), re(), U());
      }),
    m("printBgPicker") &&
      m("printBgPicker").addEventListener("input", (t) => {
        ((l.bgColor = t.target.value), re(), U());
      }),
    m("confirmDownloadSheet") &&
      m("confirmDownloadSheet").addEventListener("click", () => Ye("image")),
    m("confirmPrintPdf") && m("confirmPrintPdf").addEventListener("click", () => Ye("pdf")));
}
function Ne(t) {
  let e = t.toDataURL("image/png"),
    n = document.getElementById("a4HiddenPrintIframe");
  (n && n.remove(),
    (n = document.createElement("iframe")),
    (n.id = "a4HiddenPrintIframe"),
    (n.style.position = "fixed"),
    (n.style.right = "-9999px"),
    (n.style.bottom = "-9999px"),
    (n.style.width = "1px"),
    (n.style.height = "1px"),
    (n.style.border = "0"),
    (n.style.opacity = "0"),
    (n.style.pointerEvents = "none"),
    document.body.appendChild(n));
  let o = n.contentWindow.document;
  (o.open(),
    o.write(`<!DOCTYPE html>
<html>
<head>
  <title>Print A4 Passport Sheet</title>
  <style>
    @page { size: A4 portrait; margin: 0; }
    html, body { margin: 0; padding: 0; width: 100%; height: 100%; display: flex; align-items: center; justify-content: center; background: #fff; }
    img { max-width: 100%; max-height: 100%; object-fit: contain; }
  </style>
</head>
<body>
  <img src="${e}" onload="setTimeout(function(){ try { window.focus(); window.print(); } catch(e){} }, 250);" />
</body>
</html>`),
    o.close());
}
function Rn(t) {
  try {
    let n = `<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>Print Passport Photo Sheet (A4)</title>
  <style>
    @page { size: A4 portrait; margin: 0; }
    html, body { margin: 0; padding: 0; width: 100%; height: 100%; display: flex; align-items: center; justify-content: center; background: #ffffff; }
    img { max-width: 100%; max-height: 100%; object-fit: contain; }
    @media print {
      html, body { width: 210mm; height: 297mm; margin: 0; padding: 0; }
      img { width: 100%; height: 100%; object-fit: contain; }
    }
  </style>
</head>
<body>
  <img src="${t.toDataURL("image/png")}" id="sheetImg" alt="A4 Print Sheet" />
  <script>
    window.onload = function() {
      setTimeout(function() {
        try {
          window.focus();
          window.print();
        } catch(e) {}
      }, 300);
    };
  <\/script>
</body>
</html>`,
      o = new Blob([n], { type: "text/html;charset=utf-8" }),
      s = URL.createObjectURL(o);
    window.open(s, "_blank") || Ne(t);
  } catch (e) {
    (console.error("Direct print error:", e), Ne(t));
  }
}
function On() {
  let t = l.grid,
    e = 300,
    n = Math.round((t.sheetMM.width / 25.4) * e),
    o = Math.round((t.sheetMM.height / 25.4) * e),
    s = document.createElement("canvas");
  ((s.width = n), (s.height = o));
  let a = s.getContext("2d");
  ((a.fillStyle = "#ffffff"), a.fillRect(0, 0, n, o));
  let i = Math.round((t.marginMM / 25.4) * e),
    r = Math.round((t.marginMM / 25.4) * e),
    d = Math.round((t.gapMM / 25.4) * e),
    h = Math.round((t.gapMM / 25.4) * e),
    c = Math.round((t.photoW / 25.4) * e),
    f = Math.round((t.photoH / 25.4) * e);
  for (let u = 0; u < t.totalSlots; u++) {
    let g = u % t.cols,
      x = Math.floor(u / t.cols),
      y = i + g * (c + d),
      S = r + x * (f + h),
      M = l.slots[u];
    if (M) {
      let L = dt(l.cutout, l.spec, l.bgColor, {
        zoom: M.zoom,
        offsetX: M.offsetX,
        offsetY: M.offsetY,
      });
      (a.drawImage(L, y, S, c, f),
        (a.strokeStyle = "#cccccc"),
        (a.lineWidth = 1),
        a.strokeRect(y + 0.5, S + 0.5, c, f));
    } else
      (a.save(),
        (a.strokeStyle = "#e2e8f0"),
        (a.lineWidth = 1.5),
        a.setLineDash([4, 4]),
        a.strokeRect(y + 0.5, S + 0.5, c, f),
        a.restore());
  }
  return s;
}
async function Ye(t) {
  let e = On();
  if (t === "pdf") Rn(e);
  else {
    let n = await Ht(e, "image/png");
    ($t(n, "passport-A4-sheet.png"), le());
    let o = m("downloadModal");
    o && o.classList.remove("hidden");
  }
}
var fe = "cubit_account_v2",
  he = "cubit_history_v2";
function ft() {
  let t = Math.floor(1e3 + Math.random() * 9e3),
    e = new Date(),
    n = e.toLocaleDateString("en-US", { month: "long", year: "numeric" });
  return {
    id: `usr_${t}`,
    displayName: `Passport Creator #${t}`,
    avatar: null,
    avatarColor: "#6366f1",
    createdDate: n,
    createdTimestamp: e.toISOString(),
    stats: {
      photosProcessed: 0,
      printSheetsCreated: 0,
      singleDownloads: 0,
      lastActive: e.toISOString(),
    },
    settings: {
      defaultSpec: "bd-passport",
      defaultFormat: "png",
      autoDownload: !1,
      notifications: !0,
      theme: "dark",
    },
  };
}
function N() {
  try {
    let t = localStorage.getItem(fe);
    if (!t) {
      let n = ft();
      return (Nt(n), n);
    }
    let e = JSON.parse(t);
    return (e.stats || (e.stats = ft().stats), e.settings || (e.settings = ft().settings), e);
  } catch (t) {
    return (console.warn("Failed to load account from localStorage:", t), ft());
  }
}
function Nt(t) {
  try {
    ((t.stats.lastActive = new Date().toISOString()), localStorage.setItem(fe, JSON.stringify(t)));
  } catch (e) {
    console.error("Failed to save account:", e);
  }
}
function pe(t, e = null) {
  let n = N();
  return (t && t.trim() && (n.displayName = t.trim()), e !== void 0 && (n.avatar = e), Nt(n), n);
}
function me(t) {
  let e = N();
  return ((e.settings = { ...e.settings, ...t }), Nt(e), e);
}
function Yt(t, e = {}) {
  let n = N();
  return (
    n.stats || (n.stats = ft().stats),
    t === "photo_processed"
      ? (n.stats.photosProcessed = (n.stats.photosProcessed || 0) + 1)
      : t === "print_sheet"
        ? (n.stats.printSheetsCreated = (n.stats.printSheetsCreated || 0) + 1)
        : t === "single_download" && (n.stats.singleDownloads = (n.stats.singleDownloads || 0) + 1),
    Nt(n),
    n
  );
}
function ge() {
  try {
    let t = localStorage.getItem(he);
    return t ? JSON.parse(t) : [];
  } catch {
    return [];
  }
}
function je(t) {
  try {
    let e = ge(),
      n = {
        id: "hist_" + Date.now(),
        timestamp: new Date().toISOString(),
        formattedDate: new Date().toLocaleDateString("en-US", {
          month: "short",
          day: "numeric",
          hour: "2-digit",
          minute: "2-digit",
        }),
        specLabel: t.specLabel || "Passport Photo",
        specId: t.specId || "bd-passport",
        thumbnail: t.thumbnail || null,
      };
    return (e.unshift(n), e.length > 15 && e.pop(), localStorage.setItem(he, JSON.stringify(e)), e);
  } catch (e) {
    return (console.warn("Failed to add history item:", e), []);
  }
}
function Ge() {
  try {
    return (
      localStorage.removeItem(fe),
      localStorage.removeItem(he),
      localStorage.removeItem("cubit_photo_edits"),
      ft()
    );
  } catch (t) {
    console.error("Error clearing data:", t);
  }
}
var w = (t) => document.getElementById(t);
function V(t, e = "info", n = 3200) {
  let o = w("toastBox");
  o ||
    ((o = document.createElement("div")),
    (o.id = "toastBox"),
    (o.className = "toastBox"),
    document.body.appendChild(o));
  let s = document.createElement("div");
  s.className = `toastItem ${e}`;
  let a = {
    success:
      '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><polyline points="20 6 9 17 4 12"/></svg>',
    error:
      '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><circle cx="12" cy="12" r="10"/><line x1="12" y1="8" x2="12" y2="12"/><line x1="12" y1="16" x2="12.01" y2="16"/></svg>',
    info: '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><circle cx="12" cy="12" r="10"/><line x1="12" y1="16" x2="12" y2="12"/><line x1="12" y1="8" x2="12.01" y2="8"/></svg>',
  };
  ((s.innerHTML = `
    <span class="toastIcon">${a[e] || a.info}</span>
    <span class="toastMsg">${ht(t)}</span>
  `),
    o.appendChild(s),
    requestAnimationFrame(() => {
      s.classList.add("show");
    }),
    setTimeout(() => {
      (s.classList.remove("show"),
        setTimeout(() => {
          s.parentNode && s.parentNode.removeChild(s);
        }, 300));
    }, n));
}
function ht(t) {
  return String(t).replace(
    /[&<>"']/g,
    (e) => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;", "'": "&#39;" })[e],
  );
}
var q = null,
  z = null;
function jt(t) {
  (nt(), pt());
  let e = w(t),
    n = w("drawerBackdrop");
  e &&
    (e.classList.remove("hidden"),
    n && n.classList.remove("hidden"),
    e.offsetWidth,
    e.classList.add("open"),
    n && n.classList.add("active"),
    (q = t),
    (document.body.style.overflow = "hidden"),
    t === "accountDrawer" ? Xt() : t === "menuDrawer" && Un());
}
function nt() {
  if (!q) return;
  let t = w(q),
    e = w("drawerBackdrop");
  (t &&
    (t.classList.remove("open"),
    setTimeout(() => {
      t.classList.add("hidden");
    }, 280)),
    e &&
      !z &&
      (e.classList.remove("active"),
      setTimeout(() => {
        !q && !z && e.classList.add("hidden");
      }, 280)),
    (q = null),
    z || (document.body.style.overflow = ""));
}
function Y(t) {
  pt();
  let e = w(t),
    n = w("drawerBackdrop");
  e &&
    (e.classList.remove("hidden"),
    n && n.classList.remove("hidden"),
    e.offsetWidth,
    e.classList.add("open"),
    n && n.classList.add("active"),
    (z = t),
    (document.body.style.overflow = "hidden"),
    t === "presetsModal" ? We() : t === "historyModal" && Hn());
}
function pt() {
  if (!z) return;
  let t = w(z),
    e = w("drawerBackdrop");
  (t &&
    (t.classList.remove("open"),
    setTimeout(() => {
      t.classList.add("hidden");
    }, 280)),
    e &&
      !q &&
      (e.classList.remove("active"),
      setTimeout(() => {
        !q && !z && e.classList.add("hidden");
      }, 280)),
    (z = null),
    q || (document.body.style.overflow = ""));
}
function Xt() {
  let t = N(),
    e = w("accAvatarImg"),
    n = w("accAvatarInitials");
  t.avatar
    ? ((e.src = t.avatar), e.classList.remove("hidden"), n && n.classList.add("hidden"))
    : (e.classList.add("hidden"),
      n && (n.classList.remove("hidden"), (n.textContent = Fn(t.displayName))));
  let o = w("accNameInput");
  o && (o.value = t.displayName);
  let s = w("accCreatedDate");
  (s && (s.textContent = `Member since ${t.createdDate || "July 2026"}`),
    w("statPhotosCount") && (w("statPhotosCount").textContent = t.stats.photosProcessed || 0),
    w("statSheetsCount") && (w("statSheetsCount").textContent = t.stats.printSheetsCreated || 0),
    w("statSingleCount") && (w("statSingleCount").textContent = t.stats.singleDownloads || 0));
  let a = w("statLastActive");
  if (a) {
    let d = new Date(t.stats.lastActive || Date.now());
    a.textContent = d.toLocaleDateString("en-US", {
      month: "short",
      day: "numeric",
      hour: "2-digit",
      minute: "2-digit",
    });
  }
  let i = w("settingFormatSelect");
  i && (i.value = t.settings.defaultFormat || "png");
  let r = w("settingSpecSelect");
  (r && (r.value = t.settings.defaultSpec || "bd-passport"), mt(t));
}
function Fn(t) {
  if (!t) return "P";
  let e = t.trim().split(" ");
  return e.length >= 2 ? (e[0][0] + e[1][0]).toUpperCase() : t.slice(0, 2).toUpperCase();
}
function mt(t = N()) {
  let e = w("topProfileName"),
    n = w("topProfileSub");
  (e && (e.textContent = t.displayName),
    n && (n.textContent = `${t.stats.photosProcessed || 0} photos created`));
}
function Un() {
  let t = N(),
    e = w("menuDrawerUserName"),
    n = w("menuDrawerUserStats");
  (e && (e.textContent = t.displayName),
    n && (n.textContent = `Local Citizen \u2022 ${t.stats.photosProcessed || 0} created`));
}
function We(t = "ALL", e = "") {
  let n = w("presetsGuideGrid");
  if (!n) return;
  n.innerHTML = "";
  let o = e.toLowerCase().trim(),
    s = P.filter(
      (a) => !o || a.label.toLowerCase().includes(o) || a.country.toLowerCase().includes(o),
    );
  if (s.length === 0) {
    n.innerHTML = `<div class="emptyNotice">No passport specs found matching "${ht(e)}".</div>`;
    return;
  }
  s.forEach((a) => {
    let i = document.createElement("div");
    i.className = "presetCard";
    let r = a.id === "bd-passport";
    ((i.innerHTML = `
      <div class="presetCardHead">
        <div>
          <h4 class="presetTitle">${ht(a.label)}</h4>
          <span class="presetCountry">${ht(a.country)}</span>
        </div>
        ${r ? '<span class="badge bdBadge"><svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" style="display:inline-block; vertical-align:-1px; margin-right:3px;"><circle cx="12" cy="12" r="10"/><circle cx="12" cy="12" r="4" fill="currentColor"/></svg> Official BD</span>' : `<span class="presetDimBadge">${a.widthMM}\xD7${a.heightMM} mm</span>`}
      </div>
      <div class="presetDetails">
        <div class="presetRow"><span>Dimensions:</span> <strong>${a.widthMM} \xD7 ${a.heightMM} mm</strong></div>
        <div class="presetRow"><span>Resolution:</span> <strong>${a.dpi} DPI (300 DPI Standard)</strong></div>
        <div class="presetRow"><span>Head Fill:</span> <strong>${Math.round((a.subjectFill || 0.65) * 100)}% Frame Height</strong></div>
        <div class="presetRow"><span>Background:</span> <strong>Plain White or Sky Blue</strong></div>
      </div>
      <button class="btn primarySmall usePresetBtn" data-spec-id="${a.id}">
        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2"><polyline points="20 6 9 17 4 12"/></svg>
        Apply This Spec
      </button>
    `),
      n.appendChild(i));
  });
}
function Hn() {
  let t = w("historyList");
  if (!t) return;
  let e = ge();
  if (((t.innerHTML = ""), e.length === 0)) {
    t.innerHTML = `
      <div class="emptyHistory">
        <div class="emptyIcon"><svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="3" width="18" height="18" rx="2"/><circle cx="8.5" cy="8.5" r="1.5"/><path d="m21 15-5-5-9 9"/></svg></div>
        <h3>No saved photos yet</h3>
        <p class="muted">Your processed photos will be saved locally on this device for easy access.</p>
      </div>
    `;
    return;
  }
  e.forEach((n) => {
    let o = document.createElement("div");
    ((o.className = "historyCard"),
      (o.innerHTML = `
      <div class="historyThumb">
        ${n.thumbnail ? `<img src="${n.thumbnail}" alt="Thumbnail" />` : '<div class="noThumb">Photo</div>'}
      </div>
      <div class="historyMeta">
        <div class="historyTitle">${ht(n.specLabel)}</div>
        <div class="historyDate">${ht(n.formattedDate)}</div>
      </div>
      <div class="historyActions">
        ${n.thumbnail ? `<a href="${n.thumbnail}" download="${n.specId}-photo.png" class="btn ghostSmall">Download</a>` : ""}
      </div>
    `),
      t.appendChild(o));
  });
}
function qe(t) {
  let e = w("drawerBackdrop");
  (e &&
    e.addEventListener("click", () => {
      (nt(), pt());
    }),
    document.querySelectorAll("[data-close-drawer]").forEach((c) => {
      c.addEventListener("click", nt);
    }),
    document.querySelectorAll("[data-close-modal]").forEach((c) => {
      c.addEventListener("click", pt);
    }),
    document.addEventListener("keydown", (c) => {
      c.key === "Escape" && (nt(), pt());
    }));
  let n = w("accSaveNameBtn"),
    o = w("accNameInput");
  n &&
    o &&
    n.addEventListener("click", () => {
      let c = o.value.trim();
      c && (pe(c), V("Display name updated successfully!", "success"), Xt());
    });
  let s = w("accAvatarFile");
  s &&
    s.addEventListener("change", (c) => {
      let f = c.target.files?.[0];
      if (f) {
        let u = new FileReader();
        ((u.onload = (g) => {
          (pe(null, g.target.result), V("Profile avatar updated!", "success"), Xt());
        }),
          u.readAsDataURL(f));
      }
    });
  let a = w("accClearDataBtn");
  a &&
    a.addEventListener("click", () => {
      confirm(
        "Are you sure you want to clear all locally saved statistics, history, and settings on this device?",
      ) && (Ge(), V("All local account data cleared.", "info"), Xt(), nt());
    });
  let i = w("settingFormatSelect");
  i &&
    i.addEventListener("change", (c) => {
      (me({ defaultFormat: c.target.value }),
        V(`Default export format set to ${c.target.value.toUpperCase()}`, "success"));
    });
  let r = w("settingSpecSelect");
  r &&
    r.addEventListener("change", (c) => {
      (me({ defaultSpec: c.target.value }), V("Default passport specification saved.", "success"));
    });
  let d = w("presetSearchInput");
  d &&
    d.addEventListener("input", (c) => {
      We("ALL", c.target.value);
    });
  let h = w("presetsGuideGrid");
  (h &&
    t &&
    h.addEventListener("click", (c) => {
      let f = c.target.closest(".usePresetBtn");
      if (f) {
        let u = f.dataset.specId;
        (t(u), pt(), V(`Applied preset: ${u}`, "success"));
      }
    }),
    mt());
}
var p = (t) => document.getElementById(t);
function Dt(t) {
  let e = parseFloat(t.min),
    n = parseFloat(t.max),
    o = parseFloat(t.value),
    s = n > e ? ((o - e) / (n - e)) * 100 : 0;
  t.style.background = `linear-gradient(to right, var(--primary) ${s}%, oklch(0.34 0.05 300 / 0.9) ${s}%)`;
}
var ze = "upload",
  A = null,
  kt = null,
  we = null,
  Gt = null,
  X = P[0].id,
  ot = Ft[0].color,
  j = { ...Ct },
  Q = Fe(),
  Ve = !0,
  gt = "png",
  Pt = 4,
  Wt = null,
  wt = null,
  ye = !1,
  qt = N();
(qt?.settings?.defaultSpec && (X = qt.settings.defaultSpec),
  qt?.settings?.defaultFormat && (gt = qt.settings.defaultFormat));
var $n = { upload: p("uploadView"), processing: p("processingView"), result: p("resultView") };
function Tt(t) {
  ze = t;
  for (let [e, n] of Object.entries($n)) n.classList.toggle("hidden", e !== t);
  (document
    .querySelectorAll("[data-home-only]")
    .forEach((e) => e.classList.toggle("hidden", t !== "upload")),
    t === "upload" && !Bt && Qe(),
    t !== "upload" && Bt && (Bt(), (Bt = null)));
}
var Bt = null;
function Qe() {
  Bt = ke(p("crystalCanvas"), p("crystalBox"));
}
Qe();
var Ke = p("profileBtn"),
  Je = p("menuBtn");
(Ke &&
  Ke.addEventListener("click", (t) => {
    (t.stopPropagation(), jt("accountDrawer"));
  }),
  Je &&
    Je.addEventListener("click", (t) => {
      (t.stopPropagation(), jt("menuDrawer"));
    }),
  document.querySelectorAll("[data-menu-action]").forEach((t) => {
    t.addEventListener("click", () => {
      let e = t.dataset.menuAction;
      (nt(),
        e === "new"
          ? Zt()
          : e === "specs"
            ? Y("presetsModal")
            : e === "history"
              ? Y("historyModal")
              : e === "account"
                ? jt("accountDrawer")
                : e === "howItWorks"
                  ? Y("howItWorksModal")
                  : e === "faq"
                    ? Y("faqModal")
                    : e === "privacy"
                      ? Y("privacyModal")
                      : e === "tos" && Y("tosModal"));
    });
  }));
var Ze = p("footerPrivacyBtn");
Ze && Ze.addEventListener("click", () => Y("privacyModal"));
var tn = p("footerTosBtn");
(tn && tn.addEventListener("click", () => Y("tosModal")),
  qe((t) => {
    t && ((X = t), at && (at.value = X), bt());
  }));
var K = p("drop"),
  zt = p("fileInput"),
  en = p("pickBtn"),
  Vt = p("uploadError");
(en &&
  en.addEventListener("click", (t) => {
    (t.stopPropagation(), zt.click());
  }),
  K &&
    (K.addEventListener("click", () => zt.click()),
    K.addEventListener("dragover", (t) => {
      (t.preventDefault(), K.classList.add("drag"));
    }),
    K.addEventListener("dragleave", () => K.classList.remove("drag")),
    K.addEventListener("drop", (t) => {
      (t.preventDefault(), K.classList.remove("drag"));
      let e = t.dataTransfer.files?.[0];
      e && nn(e);
    })),
  zt &&
    zt.addEventListener("change", (t) => {
      let e = t.target.files?.[0];
      (e && nn(e), (t.target.value = ""));
    }));
async function _n(t) {
  let e = await createImageBitmap(t, { imageOrientation: "from-image" }),
    o = Math.min(1, 2e3 / Math.max(e.width, e.height)),
    s = Math.max(1, Math.round(e.width * o)),
    a = Math.max(1, Math.round(e.height * o)),
    i = document.createElement("canvas");
  ((i.width = s), (i.height = a));
  let r = i.getContext("2d");
  return ((r.imageSmoothingQuality = "high"), r.drawImage(e, 0, 0, s, a), e.close(), i);
}
function yt(t, e) {
  if (!e) {
    t.classList.add("hidden");
    return;
  }
  ((t.textContent = e), t.classList.remove("hidden"));
}
async function nn(t) {
  if (!t.type.startsWith("image/")) {
    yt(Vt, "Please choose an image file (JPG or PNG).");
    return;
  }
  (yt(Vt, null), (Wt = null), (wt = null), (j = { ...Ct }), Tt("processing"), on("download", null));
  try {
    let e = performance.now(),
      n = await _n(t);
    Gt = n;
    let o = await ae(n, (a) =>
      on(a.stage, a.total ? Math.round((a.loaded / a.total) * 100) : null),
    );
    ((A = se(n, o.maskCanvas, n.width, n.height)),
      (kt = A.canvas),
      (Wt = o.backend),
      (wt = { inference: o.inferenceMs, total: performance.now() - e }),
      an(),
      Yt("photo_processed"));
    let s = P.find((a) => a.id === X) || P[0];
    try {
      let a = A.canvas.toDataURL("image/png");
      je({ specLabel: s.label, specId: s.id, thumbnail: a });
    } catch (a) {
      console.warn("Could not generate history thumbnail:", a);
    }
    (Tt("result"), be(), xe(), bt(), mt());
  } catch (e) {
    (console.error(e),
      yt(Vt, e instanceof Error ? e.message : "Something went wrong while processing."),
      Tt("upload"));
  }
}
function on(t, e) {
  let n = p("procTitle"),
    o = p("procSub");
  !n ||
    !o ||
    (t === "download"
      ? ((n.textContent = "Warming up the AI\u2026"),
        (o.textContent = "Preparing your photo studio."))
      : t === "compile"
        ? ((n.textContent = "Tuning the studio lights\u2026"),
          (o.textContent = "Getting everything ready for you."))
        : t === "ready"
          ? ((n.textContent = "Polishing your portrait\u2026"), (o.textContent = "Almost there."))
          : t === "done" &&
            ((n.textContent = "Finishing up\u2026"), (o.textContent = "Almost there.")));
}
function an() {
  let t = p("backendBadge");
  t &&
    (Wt === "webgpu"
      ? ((t.innerHTML =
          '<svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" style="display:inline-block; vertical-align:-1px; margin-right:3px;"><polygon points="13 2 3 14 12 14 11 22 21 10 12 10 13 2"/></svg> WebGPU'),
        t.classList.remove("hidden"))
      : ((t.textContent = ""), t.classList.add("hidden")));
}
var at = p("specSelect"),
  sn = p("specMeta");
at &&
  (P.forEach((t) => {
    let e = document.createElement("option");
    ((e.value = t.id), (e.textContent = t.label), at.appendChild(e));
  }),
  (at.value = X),
  at.addEventListener("change", () => {
    ((X = at.value), bt());
  }));
var At = p("bgSwatches");
if (At) {
  Ft.forEach((n) => {
    let o = document.createElement("button");
    ((o.className = "sw"),
      (o.style.backgroundColor = n.color),
      (o.title = n.label),
      o.addEventListener("click", () => {
        ((ot = n.color), bt());
      }),
      At.appendChild(o));
  });
  let t = document.createElement("label");
  ((t.className = "swColor"), (t.title = "Custom color"), (t.textContent = "+"));
  let e = document.createElement("input");
  ((e.type = "color"),
    (e.value = ot),
    e.addEventListener("input", (n) => {
      ((ot = n.target.value), bt());
    }),
    t.appendChild(e),
    At.appendChild(t));
}
(document.querySelectorAll(".fmtBtn").forEach((t) => {
  t.addEventListener("click", () => {
    ((gt = t.dataset.fmt),
      document.querySelectorAll(".fmtBtn").forEach((e) => e.classList.toggle("active", e === t)));
  });
}),
  document.querySelectorAll(".qtyBtn").forEach((t) => {
    t.addEventListener("click", () => {
      ((Pt = parseInt(t.dataset.count, 10) || 4),
        document.querySelectorAll(".qtyBtn").forEach((n) => n.classList.toggle("active", n === t)));
      let e = p("qtyCustomInput");
      e && (e.value = Pt);
    });
  }));
var rn = p("qtyCustomInput");
rn &&
  rn.addEventListener("input", (t) => {
    let e = parseInt(t.target.value, 10);
    !isNaN(e) &&
      e >= 1 &&
      ((Pt = Math.min(60, e)),
      document
        .querySelectorAll(".qtyBtn")
        .forEach((n) => n.classList.toggle("active", parseInt(n.dataset.count, 10) === Pt)));
  });
var J = p("zoom"),
  st = p("offY"),
  it = p("offX"),
  Nn = p("zoomVal"),
  Yn = p("offYVal"),
  Xn = p("offXVal");
function vt() {
  !J ||
    !st ||
    !it ||
    ((Nn.textContent = (+J.value).toFixed(2) + "\xD7"),
    (Yn.textContent = Math.round(+st.value * 100)),
    (Xn.textContent = Math.round(+it.value * 100)),
    [J, st, it].forEach(Dt));
}
(J &&
  J.addEventListener("input", (t) => {
    ((j.zoom = +t.target.value), vt(), lt());
  }),
  st &&
    st.addEventListener("input", (t) => {
      ((j.offsetY = +t.target.value), vt(), lt());
    }),
  it &&
    it.addEventListener("input", (t) => {
      ((j.offsetX = +t.target.value), vt(), lt());
    }),
  p("guidesChk") &&
    p("guidesChk").addEventListener("change", (t) => {
      ((Ve = t.target.checked), lt());
    }),
  p("resetFrame") &&
    p("resetFrame").addEventListener("click", () => {
      ((j = { ...Ct }), (J.value = 1), (st.value = 0), (it.value = 0), vt(), lt());
    }),
  vt(),
  p("newBtn") && p("newBtn").addEventListener("click", Zt),
  p("dlSingle") && p("dlSingle").addEventListener("click", () => Se("single")),
  p("dlSheet") && p("dlSheet").addEventListener("click", () => Se("sheet")),
  p("dlPrint") && p("dlPrint").addEventListener("click", () => Se("print")));
var rt = p("downloadModal");
function jn() {
  rt && rt.classList.remove("hidden");
}
function ve() {
  rt && rt.classList.add("hidden");
}
(rt &&
  rt.addEventListener("click", (t) => {
    t.target === rt && ve();
  }),
  p("modalNewImage") &&
    p("modalNewImage").addEventListener("click", () => {
      (ve(), Zt());
    }),
  p("modalHome") &&
    p("modalHome").addEventListener("click", () => {
      (ve(), Zt());
    }));
var Gn = {
    brightness: "Brightness",
    contrast: "Contrast",
    exposure: "Exposure",
    highlights: "Highlights",
    shadows: "Shadows",
    clarity: "Clarity",
    sharpness: "Sharpness",
    noise: "Denoise",
  },
  ln = p("editorSliders"),
  Qt = {},
  Kt = {};
(ln &&
  ut.forEach((t) => {
    let e = document.createElement("div");
    ((e.className = "editRow"),
      (e.innerHTML = `
      <span class="editLabel">${Gn[t]}</span>
      <input type="range" min="-100" max="100" step="1" value="0" data-k="${t}" />
      <span class="editValue" data-k="${t}">0</span>
    `),
      ln.appendChild(e));
    let n = e.querySelector("input");
    ((Qt[t] = n),
      (Kt[t] = e.querySelector(".editValue")),
      Dt(n),
      n.addEventListener("input", (o) => {
        ((Q[t] = +o.target.value), (Kt[t].textContent = Q[t]), Dt(n), Ee());
      }),
      n.addEventListener("dblclick", () => {
        ((Q[t] = 0), (n.value = 0), (Kt[t].textContent = 0), Dt(n), Ee());
      }));
  }),
  p("resetEdits") &&
    p("resetEdits").addEventListener("click", () => {
      ((Q = { ...It }), be(), ie(Q), Ee());
    }));
function be() {
  ut.forEach((t) => {
    let e = Q[t] || 0;
    Qt[t] && ((Qt[t].value = e), (Kt[t].textContent = e), Dt(Qt[t]));
  });
}
var Jt = null;
function Ee() {
  (ie(Q),
    Jt && clearTimeout(Jt),
    (Jt = setTimeout(() => {
      ((Jt = null), xe(), lt());
    }, 90)));
}
function xe() {
  !kt || !A || ((we = Ue(kt, Q)), (A.canvas = we));
}
function Zt() {
  ((A = null), (kt = null), (we = null), yt(Vt, null), Tt("upload"));
}
function bt() {
  let t = P.find((n) => n.id === X) || P[0],
    e = Mt(t);
  (sn &&
    (sn.textContent = `${t.widthMM}\xD7${t.heightMM} mm \xB7 ${e.width}\xD7${e.height}px \xB7 ${t.dpi} DPI`),
    At &&
      Array.from(At.querySelectorAll(".sw")).forEach((n, o) =>
        n.classList.toggle("active", Ft[o].color === ot),
      ),
    wt &&
      p("timings") &&
      (p("timings").textContent =
        `Processed in ${(wt.total / 1e3).toFixed(2)}s \xB7 AI inference ${Math.round(wt.inference)}ms`),
    lt());
}
function lt() {
  if (ze !== "result" || !A) return;
  let t = P.find((i) => i.id === X) || P[0],
    e = dt(A, t, ot, j),
    n = p("previewCanvas");
  if (!n) return;
  let o = 380,
    s = Math.min(o / e.width, o / e.height, 1.4);
  ((n.width = Math.round(e.width * s)), (n.height = Math.round(e.height * s)));
  let a = n.getContext("2d");
  if (
    ((a.imageSmoothingQuality = "high"),
    a.clearRect(0, 0, n.width, n.height),
    a.drawImage(e, 0, 0, n.width, n.height),
    Ve)
  ) {
    let i = n.width,
      r = n.height;
    (a.save(),
      (a.strokeStyle = "rgba(192, 100, 250, 0.6)"),
      a.setLineDash([6, 5]),
      (a.lineWidth = 1),
      [0.1, 0.86].forEach((d) => {
        (a.beginPath(), a.moveTo(0, r * d), a.lineTo(i, r * d), a.stroke());
      }),
      a.beginPath(),
      a.moveTo(i / 2, 0),
      a.lineTo(i / 2, r),
      a.stroke(),
      a.restore());
  }
}
async function Se(t) {
  if (!A || ye) return;
  let e = P.find((n) => n.id === X) || P[0];
  if (t === "sheet" || t === "print") {
    (Yt("print_sheet"),
      Xe({ cutout: A, spec: e, bgColor: ot, adjust: j, count: Pt, format: gt, pageSize: "4x6" }),
      mt());
    return;
  }
  ((ye = !0),
    p("dlSingle") && (p("dlSingle").disabled = !0),
    p("dlSheet") && (p("dlSheet").disabled = !0),
    p("dlPrint") && (p("dlPrint").disabled = !0),
    yt(p("resultError"), null));
  try {
    let n = dt(A, e, ot, j),
      o = gt === "png" ? "image/png" : "image/jpeg",
      s = gt === "png" ? "png" : "jpg",
      a = await Ht(n, o, gt === "jpeg" ? 0.95 : void 0),
      i = `passport-${e.id}.${s}`;
    ($t(a, i), Yt("single_download"), mt(), V("Photo downloaded successfully!", "success"), jn());
  } catch (n) {
    (console.error(n),
      yt(p("resultError"), n instanceof Error ? n.message : "Could not export the image."));
  } finally {
    ((ye = !1),
      p("dlSingle") && (p("dlSingle").disabled = !1),
      p("dlSheet") && (p("dlSheet").disabled = !1),
      p("dlPrint") && (p("dlPrint").disabled = !1));
  }
}
function Wn(t) {
  let n = Math.min(1, 2e3 / Math.max(t.width, t.height)),
    o = Math.max(1, Math.round(t.width * n)),
    s = Math.max(1, Math.round(t.height * n)),
    a = document.createElement("canvas");
  ((a.width = o), (a.height = s));
  let i = a.getContext("2d");
  return ((i.imageSmoothingQuality = "high"), i.drawImage(t, 0, 0, o, s), a);
}
window.__tryOn = {
  getPersonDataUrl() {
    if (Gt)
      try {
        return Gt.toDataURL("image/png");
      } catch {}
    let t = p("previewCanvas");
    if (t)
      try {
        return t.toDataURL("image/png");
      } catch {}
    return null;
  },
  async applyResult(t, e) {
    let n = new Image();
    ((n.crossOrigin = "anonymous"),
      await new Promise((a, i) => {
        ((n.onload = a),
          (n.onerror = () => i(new Error("Failed to load result image"))),
          (n.src = t));
      }));
    let o = Wn(n);
    ((Gt = o), e && e("Removing background\u2026"));
    let s = await ae(o, () => {});
    ((A = se(o, s.maskCanvas, o.width, o.height)),
      (kt = A.canvas),
      (Wt = s.backend),
      (wt = { inference: s.inferenceMs, total: 0 }),
      an(),
      (j = { ...Ct }),
      J && ((J.value = 1), (st.value = 0), (it.value = 0), vt()),
      Tt("result"),
      be(),
      xe(),
      bt());
  },
};
var qn = "/api/public/tryon",
  Yo = 18e4,
  zn = [
    { id: "formal", label: "Formal", img: "/garments/1.png", description: "a formal shirt" },
    {
      id: "whitetshirt",
      label: "White T-Shirt",
      img: "/garments/2.png",
      description: "a white t-shirt",
    },
    { id: "shirt", label: "Shirt", img: "/garments/3.png", description: "a shirt" },
    { id: "tshirt", label: "T-Shirt", img: "/garments/4.png", description: "a t-shirt" },
    {
      id: "traditional",
      label: "Traditional",
      img: "/garments/5.png",
      description: "a traditional outfit",
    },
  ],
  ct = null,
  Z = !1,
  Rt = null,
  cn = null,
  Me = "",
  te = null,
  H = (t) => document.getElementById(t),
  Vn =
    '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><line x1="12" y1="5" x2="12" y2="19"/><line x1="5" y1="12" x2="19" y2="12"/></svg>';
function T(t, e = "") {
  let n = H("tryOnStatus");
  n && ((n.textContent = t || ""), (n.className = "tryOnStatus " + e));
}
function G() {
  let t = H("tryOnGenerate");
  if (!t) return;
  let e = !!Rt || Me.trim().length > 0,
    n = !Z && !!ct && (ct.id !== "custom" || e) && !!window.__tryOn?.getPersonDataUrl?.();
  ((t.disabled = !n), (t.textContent = Z ? "Generating\u2026" : "Change clothes"));
}
function Qn() {
  let t = H("tryOnDresses");
  if (!t) return;
  ((t.innerHTML = ""),
    [...zn, { id: "custom", label: "Custom", img: null }].forEach((n) => {
      let o = document.createElement("button");
      ((o.type = "button"), (o.className = "tryOnDress"), (o.dataset.id = n.id));
      let s = n.img ? n.img.replace("/garments/", "/") : "",
        a = n.img ? n.img.replace("/garments/", "/assets/") : "",
        i = n.img
          ? `<img src="${n.img}" alt="${n.label}" loading="lazy" onerror="if(!this.dataset.tried1){this.dataset.tried1='1';this.src='${s}';}else if(!this.dataset.tried2){this.dataset.tried2='1';this.src='${a}';}" />`
          : Vn;
      ((o.innerHTML = `<span class="tryOnDressIcon">${i}</span><span class="tryOnDressLabel">${n.label}</span>`),
        (o.onclick = () => {
          ((ct = n),
            t
              .querySelectorAll(".tryOnDress")
              .forEach((r) => r.classList.toggle("active", r.dataset.id === n.id)),
            H("tryOnCustom").classList.toggle("hidden", n.id !== "custom"),
            G());
        }),
        t.appendChild(o));
    }));
}
async function Kn(t) {
  let e = await fetch(t);
  if (!e.ok) {
    let n = t.startsWith("/") ? t.slice(1) : "/" + t;
    e = await fetch(n);
  }
  if (!e.ok && t.includes("/garments/")) {
    let n = t.replace("/garments/", "/");
    e = await fetch(n);
  }
  if (!e.ok && t.includes("/garments/")) {
    let n = t.replace("/garments/", "/assets/");
    e = await fetch(n);
  }
  if (!e.ok) throw new Error(`Failed to load garment (${e.status})`);
  return await e.blob();
}
async function Jn(t) {
  return await (await fetch(t)).blob();
}
async function Zn(t) {
  return await new Promise((e, n) => {
    let o = new FileReader();
    ((o.onload = () => e(o.result)),
      (o.onerror = () => n(new Error("Could not read image"))),
      o.readAsDataURL(t));
  });
}
async function to(t, e, n) {
  let o = new FormData();
  (o.append("person", t, "person.png"),
    o.append("garment", e, "garment.png"),
    o.append("description", n || "a garment"));
  let s = new AbortController(),
    a = setTimeout(() => s.abort("timeout"), 18e4),
    i;
  try {
    i = await fetch(qn, { method: "POST", body: o, signal: s.signal });
  } catch (d) {
    throw (
      clearTimeout(a),
      d?.name === "AbortError"
        ? new Error("Request timed out \u2014 the AI Space may be waking up.")
        : new Error("Network error contacting the AI service.")
    );
  }
  clearTimeout(a);
  let r = i.headers.get("content-type") || "";
  if (!i.ok) {
    let d = `Request failed (${i.status})`;
    if (r.includes("application/json"))
      try {
        let h = await i.json();
        h?.error && (d = h.error);
      } catch {}
    throw new Error(d);
  }
  if (!r.startsWith("image/")) throw new Error("Unexpected response from AI service.");
  return await i.blob();
}
async function eo() {
  if (Z || !ct) return;
  if (!window.__tryOn) {
    T("Editor not ready yet \u2014 upload a photo first.", "err");
    return;
  }
  let t = window.__tryOn.getPersonDataUrl();
  if (!t) {
    T("Upload a person photo first.", "err");
    return;
  }
  let e,
    n = "a garment";
  try {
    if (ct.id === "custom") {
      let o = Me.trim();
      if (!Rt && !o) {
        T("Upload a garment image or enter a prompt.", "err");
        return;
      }
      (Rt ? (e = Rt) : (e = await oo()), o && (n = o));
    } else ((e = await Kn(ct.img)), (n = ct.description));
  } catch (o) {
    T(o.message || "Could not load garment.", "err");
    return;
  }
  ((Z = !0), G(), T("Sending photo & garment to AI\u2026"));
  try {
    te = t;
    let o = await Jn(t),
      s = await to(o, e, n);
    T("Preparing your new photo\u2026");
    let a = await Zn(s);
    (await window.__tryOn.applyResult(a, (i) => T(i)),
      H("tryOnRevert").classList.remove("hidden"),
      T("Done. Continue editing, cropping or downloading.", "ok"));
  } catch (o) {
    (console.error(o), T(o.message || "Generation failed. Please try again.", "err"));
  } finally {
    ((Z = !1), G());
  }
}
async function no() {
  if (!(!te || Z)) {
    ((Z = !0), G(), T("Restoring original\u2026"));
    try {
      (await window.__tryOn.applyResult(te, (t) => T(t)),
        H("tryOnRevert").classList.add("hidden"),
        (te = null),
        T("Reverted to original.", "ok"));
    } catch (t) {
      T(t.message || "Could not revert.", "err");
    } finally {
      ((Z = !1), G());
    }
  }
}
async function oo() {
  let t = document.createElement("canvas");
  ((t.width = 512), (t.height = 512));
  let e = t.getContext("2d");
  return (
    (e.fillStyle = "#ffffff"),
    e.fillRect(0, 0, t.width, t.height),
    await new Promise((n) => t.toBlob((o) => n(o), "image/png"))
  );
}
function ao() {
  let t = H("tryOnCustomFile"),
    e = H("tryOnCustomPreview"),
    n = H("tryOnCustomFileLabel"),
    o = H("tryOnCustomPrompt");
  (t &&
    t.addEventListener("change", (s) => {
      let a = s.target.files?.[0];
      if (!a) return;
      if (!a.type.startsWith("image/")) {
        T("Please choose an image file.", "err");
        return;
      }
      Rt = a;
      let i = new FileReader();
      ((i.onload = () => {
        ((cn = i.result),
          (e.src = cn),
          e.classList.remove("hidden"),
          (n.textContent = "Change garment image"),
          G());
      }),
        i.readAsDataURL(a));
    }),
    o &&
      o.addEventListener("input", (s) => {
        ((Me = s.target.value || ""), G());
      }));
}
function dn() {
  (Qn(), ao(), (H("tryOnGenerate").onclick = eo), (H("tryOnRevert").onclick = no));
  let t = new MutationObserver(G),
    e = document.getElementById("resultView");
  (e && t.observe(e, { attributes: !0, attributeFilter: ["class"] }), G());
}
document.readyState === "loading" ? document.addEventListener("DOMContentLoaded", dn) : dn();
var so = "/api/public/enhance",
  Xo = 23e4,
  Le = 2,
  tt = !1,
  ee = null,
  Et = (t) => document.getElementById(t);
function _(t, e = "") {
  let n = Et("enhanceStatus");
  n && ((n.textContent = t || ""), (n.className = "tryOnStatus " + e));
}
function xt() {
  let t = Et("enhanceGenerate");
  if (!t) return;
  let e = !tt && !!window.__tryOn?.getPersonDataUrl?.();
  ((t.disabled = !e), (t.textContent = tt ? "Enhancing\u2026" : "Enhance photo"));
}
async function io(t) {
  return await (await fetch(t)).blob();
}
async function ro(t) {
  return await new Promise((e, n) => {
    let o = new FileReader();
    ((o.onload = () => e(o.result)),
      (o.onerror = () => n(new Error("Could not read image"))),
      o.readAsDataURL(t));
  });
}
async function lo(t, e) {
  let n = new FormData();
  (n.append("image", t, "image.png"), n.append("upscale", String(e)));
  let o = new AbortController(),
    s = setTimeout(() => o.abort("timeout"), 23e4),
    a;
  try {
    a = await fetch(so, { method: "POST", body: n, signal: o.signal });
  } catch (r) {
    throw (
      clearTimeout(s),
      r?.name === "AbortError"
        ? new Error("Request timed out \u2014 the AI Space may be waking up.")
        : new Error("Network error contacting the enhancer.")
    );
  }
  clearTimeout(s);
  let i = a.headers.get("content-type") || "";
  if (!a.ok) {
    let r = `Enhance failed (${a.status})`;
    if (i.includes("application/json"))
      try {
        let d = await a.json();
        d?.error && (r = d.error);
      } catch {}
    throw new Error(r);
  }
  if (!i.startsWith("image/")) throw new Error("Unexpected response from enhancer.");
  return await a.blob();
}
async function co() {
  if (tt) return;
  if (!window.__tryOn) {
    _("Editor not ready \u2014 upload a photo first.", "err");
    return;
  }
  let t = window.__tryOn.getPersonDataUrl();
  if (!t) {
    _("Upload a photo first.", "err");
    return;
  }
  ((tt = !0), xt(), _(`Enhancing at ${Le}x\u2026`));
  try {
    ee = t;
    let e = await io(t),
      n = await lo(e, Le);
    _("Preparing enhanced photo\u2026");
    let o = await ro(n);
    (await window.__tryOn.applyResult(o, (s) => _(s)),
      Et("enhanceRevert").classList.remove("hidden"),
      _("Enhanced. Keep editing or download.", "ok"));
  } catch (e) {
    (console.error(e), _(e.message || "Enhance failed. Please try again.", "err"));
  } finally {
    ((tt = !1), xt());
  }
}
async function uo() {
  if (!(!ee || tt)) {
    ((tt = !0), xt(), _("Restoring original\u2026"));
    try {
      (await window.__tryOn.applyResult(ee, (t) => _(t)),
        Et("enhanceRevert").classList.add("hidden"),
        (ee = null),
        _("Reverted to original.", "ok"));
    } catch (t) {
      _(t.message || "Could not revert.", "err");
    } finally {
      ((tt = !1), xt());
    }
  }
}
function un() {
  document.querySelectorAll(".enhanceScaleBtn").forEach((s) => {
    s.addEventListener("click", () => {
      let a = String(s.dataset.scale || "2x").replace("x", "");
      ((Le = Math.min(4, Math.max(1, parseInt(a, 10) || 2))),
        document
          .querySelectorAll(".enhanceScaleBtn")
          .forEach((i) => i.classList.toggle("active", i === s)));
    });
  });
  let t = Et("enhanceGenerate"),
    e = Et("enhanceRevert");
  (t && (t.onclick = co), e && (e.onclick = uo));
  let n = new MutationObserver(xt),
    o = document.getElementById("resultView");
  (o && n.observe(o, { attributes: !0, attributeFilter: ["class"] }), xt());
}
document.readyState === "loading" ? document.addEventListener("DOMContentLoaded", un) : un();
